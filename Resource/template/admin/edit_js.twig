<script src="{{ asset('NewsEx4/assets/ckeditor/ckeditor.js', 'plugin') }}"></script>
<script>
    $(function() {
        var options = {
            filebrowserUploadUrl: '{{ url('plugin_ckeditor_upload_image') }}',
            filebrowserUploadMethod: 'form',
            image_previewText: '',
            enterMode : CKEDITOR.ENTER_BR,
            shiftEnterMode: CKEDITOR.ENTER_P,
            allowedContent: true,
            height: 500,
        }

        CKEDITOR.replace( 'admin_news[description]', options);
    });
</script>
